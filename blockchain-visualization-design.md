# 区块链可视化网站 — 设计文档

## 项目概述

基于 [Anders Brownworth's Blockchain Demo](https://andersbrownworth.com/blockchain) 进行现代化重构，使用 React + Canvas + Vite 技术栈，全面提升交互体验和可视化效果。

原始仓库: [github.com/anders94/blockchain-demo](https://github.com/anders94/blockchain-demo)

---

## 技术栈

| 层 | 技术 |
|---|---|
| **构建工具** | Vite |
| **前端框架** | React 18+ (TypeScript) |
| **渲染** | Canvas 2D API (手绘粒子/连线) + React DOM (UI 控件) |
| **状态管理** | Zustand (轻量，无 boilerplate) |
| **加密** | Web Crypto API 或 js-sha256 (浏览器端计算) |
| **动画** | requestAnimationFrame + 自定义缓动函数 |
| **样式** | CSS Modules / Tailwind CSS |
| **路由** | React Router v6 |
| **部署** | 纯静态产物，Vercel / GitHub Pages / Nginx 均可 |

---

## 页面清单 (6 个)

导航栏从左到右递进展开:

### 1. Hash (`/hash`)

**核心概念:** 哈希函数的单向性、雪崩效应

**原始版本:** 一个 textarea 输入框 + 一个只读 hash 输出框

**增强设计:**
- **分屏布局** — 左侧输入区，右侧 64 位哈希结果用 Canvas 逐字符渲染
- **字符高亮系统** — 前导零用金色/高亮色标注，每个字符用不同色调，形成视觉指纹
- **输入→输出粒子映射** — 输入一个字符时，粒子从输入区沿管道/光束流向哈希区，展示「数据被消化」的过程
- **雪崩效应演示** — 预设两个几乎相同的输入（仅差一个字符），让用户切换查看，两个 hash 完全不同
- **实时字符变化** — 输入时，受影响的哈希字符逐个翻转（slot machine 效果）刷新

### 2. Block (`/block`)

**核心概念:** 区块结构、Nonce、工作量证明、挖矿

**原始版本:** 表单显示 Block Number、Nonce、Data、Hash，一个 Mine 按钮

**增强设计:**
- **拟物化区块卡片** — Canvas 绘制带阴影/圆角/纹理的 3D 风格卡片，包含以下区域:
  - 区块编号（左上角徽标）
  - Nonce 输入区
  - Data 文本区
  - 前一个哈希（只读）
  - 当前哈希（只读，前置零高亮）
  - Mine 按钮（Canvas 渲染的按钮，带交互态）
- **挖矿动画** — 点击 Mine 后:
  - Canvas 上 nonce 数字高速跳动（老虎机滚动效果）
  - 背景出现粒子涌动（模拟算力）
  - 找到有效 nonce 时: 烟花/光爆效果，卡片由红变绿的渐变过渡
- **难度滑块** — 可调节前导零数量，实时显示难度倍数

### 3. Blockchain (`/blockchain`)

**核心概念:** 链式结构、不可篡改性、牵一发而动全身

**原始版本:** 5 个区块水平排列，每个有独立的表单控件

**增强设计:**
- **Canvas 连线** — 每个区块的 Hash 输出端用箭头连线指向下一个区块的 Prev Hash 输入端，清晰展示链式关系
- **Hover 高亮联动** — 鼠标悬停在某个区块上时:
  - 该区块放大/高亮
  - 与其相关的前后连线同时高亮发光
  - 所有受影响的后续区块闪烁（被污染的链）
- **篡改演示模式** — 用户点击区块的 Data 区域修改内容:
  - 该区块立即变红，连线断裂（红色虚线）
  - 后续所有区块逐一变红，连线依次断裂
  - 提供「一键修复」按钮（自动重新挖矿），演示恢复链条的代价
- **水平滚动/缩放** — 区块数量多时支持画布拖拽平移和缩放
- **区块卡片交互** — 支持拖拽区块（但拖离后会断开链，需重新连接）

### 4. Distributed (`/distributed`)

**核心概念:** 分布式网络、共识、广播、分叉

**原始版本:** 3 条链（Peer A/B/C）并排显示，无网络拓扑

**增强设计:**
- **节点拓扑图** — Canvas 绘制:
  - 3 个 Peer 节点画成圆形图标（带头像/标签），分布在画面上方三角位置
  - 节点之间有半透明连线，表示网络连接
  - 每条链显示在对应节点下方
- **广播飞行动画** — 当某个节点挖出新块时:
  - 数据包（发光球体）从该节点沿连线飞向其他节点
  - 目标节点收到后更新自己的链
  - 如果其他节点也在同时挖矿，可能收到旧块 → 模拟网络延迟
- **分叉演示** — 模拟场景:
  - 两个节点同时挖出不同区块（网络延迟）
  - 短暂出现两条不同链（分叉），用不同颜色区分
  - 经过「共识」后，较长链胜出，另一条淡出
  - 整个分叉→合并过程用动画呈现
- **网络状态指示** — 节点间连线颜色表示延迟:
  - 绿色 = 正常同步
  - 黄色 = 延迟
  - 红色 = 分叉/不同步

### 5. Tokens (`/tokens`)

**核心概念:** 账户模型、代币转账、UTXO/余额

**原始版本:** 展示账户余额变化

**增强设计:**
- **用户头像 + 余额卡片** — Canvas 绘制圆形头像图标，下方显示余额数字（带动画计数器效果）
- **转账交互** — 用户可以在 UI 中选择发送方、接收方、金额:
  - 点击「发送」后，飞行动画展示代币从发送方头像飞向接收方
  - 发送方余额数字递减，接收方递增（滚动计数器）
- **交易记录** — 底部滚动展示最近交易列表，每条记录带有发送方→接收方的小箭头图标
- **余额不足提示** — 动画抖动 + 红色闪烁
- **多代币支持** — 可扩展多种代币类型

### 6. Coinbase (`/coinbase`)

**核心概念:** 挖矿奖励、币基交易、新币产生

**原始版本:** 展示挖矿时新区币的生成

**增强设计:**
- **矿池动画** — Canvas 绘制:
  - 一个矿机图标（齿轮/芯片），正在计算哈希
  - 成功挖出区块时，从矿机中「掉落」新币（金币掉落动画 + 音效可选）
  - 新币落入矿工钱包，余额增加
- **奖励递减演示** — 可选展示比特币减半机制:
  - 显示当前区块奖励（50 → 25 → 12.5 → 6.25 BTC）
  - 进度条展示下一个减半区块的距离
- **交易费可视化** — 区块中的交易以卡片流形式进入，每笔交易旁标注手续费
  - 挖到矿时，手续费卡片也飞入矿工钱包

---

## 交互模式对比

| 维度 | 原始版本 | 本项目 |
|---|---|---|
| 区块渲染 | Bootstrap well 容器 + 表单 | Canvas 拟物化卡片 + 3D 阴影 |
| 链式关系 | 靠 Prev Hash 输入框体现 | Canvas 箭头连线 + 高亮联动 |
| 挖矿反馈 | 按钮 loading 转圈，瞬间完成 | nonce 跳动 + 粒子涌动 + 成功烟花 |
| 状态反馈 | 红/绿背景瞬间切换 | 渐变色过渡 + 抖动/闪烁 + 连线变化 |
| 分布式 | 3 条链并排，无关联 | 节点拓扑图 + 飞行动画 + 分叉合并 |
| 网络 | 不存在 | 模拟延迟、广播、共识过程 |

---

## 技术架构

```
src/
├── components/
│   ├── BlockCard/          # 区块卡片组件 (Canvas + DOM 混合)
│   ├── HashDisplay/        # 哈希字符渲染
│   ├── CanvasLayer/        # Canvas 图层 (连线/粒子/动画)
│   ├── Topology/           # 分布式节点拓扑图
│   ├── MineAnimation/      # 挖矿动画
│   ├── ParticleSystem/     # 粒子效果系统
│   └── ...
├── pages/
│   ├── Hash/               # /hash
│   ├── Block/              # /block
│   ├── Blockchain/         # /blockchain
│   ├── Distributed/        # /distributed
│   ├── Tokens/             # /tokens
│   └── Coinbase/           # /coinbase
├── store/
│   ├── blockchain.ts       # 区块状态 (Zustand)
│   ├── mining.ts           # 挖矿状态
│   └── network.ts          # 分布式网络状态
├── utils/
│   ├── sha256.ts           # 哈希计算
│   ├── mining.ts           # 挖矿算法 (可放 Worker)
│   └── animations.ts       # 缓动函数 + 动画工具
├── workers/
│   └── mining.worker.ts    # Web Worker: 挖矿计算
└── App.tsx
```

---

## 渲染分层策略

- **底层 (Canvas)** — 连线、粒子、飞行动画、背景装饰、节点拓扑
- **中层 (React DOM)** — 区块卡片容器、输入框、按钮、文字标签
- **顶层 (Canvas overlay)** — 粒子特效覆盖在 DOM 之上（通过 pointer-events: none）

两层 Canvas 分离: 静态层（连线/区块）和动效层（粒子/飞行），减少重绘。

---

## 开发建议

### 性能
- 挖矿计算放入 Web Worker，不阻塞 UI 动画
- Canvas 分层: 静态内容用离屏 Canvas 缓存，只重绘动效层
- 大量粒子时使用 `Uint8Array` 存储位置数据，减少 GC

### 体验
- 移动端适配: Canvas 响应式缩放，触摸事件支持
- 暗色模式: 默认暗色主题，更贴合区块链的科技感
- 音效（可选）: 挖矿成功/转账完成时播放简短音效

### 扩展
- 预留 API 接口，后续可连接真实区块链节点获取数据
- 可导出当前状态为图片（`canvas.toDataURL()`）
